# Research Handoff - 2026-09-15

## Current status

Project: `reusahn/interactive-digital-humans`

Current research question:

> Do small learned cross-joint LBS components in HUGS causally mediate the stable contralateral upper-body response observed under a local left-wrist perturbation, and does that mechanism survive changes in base pose and independently pretrained sequences?

For the pretrained NeuMan Seattle model, the fixed counterfactual answer is yes. The effect is also stable across four tested Seattle evaluation poses. Independent-model replication has not yet been completed.

## Established Seattle result

Key correction:

```text
probe frame_index=0 -> raw NeuMan frame 2, not 22
```

Corrected high-confidence contralateral definition:

```text
right_collar + right_shoulder + right_elbow + right_wrist + right_hand
N = 33,072
```

Same-side local chain:

```text
left_collar + left_shoulder + left_elbow
```

Step 14E raw-frame-2 counterfactual:

```text
learned LBS contralateral displacement: 4.163576534949515
K6 target contralateral displacement:    0.007555978789000772
selective wrist/hand ablation:           0.0
K6 reduction vs learned:                 99.81852191918234%
selective ablation vs learned:            100.0%
removed-mass vs displacement-reduction r: 0.9812889009199712
```

Step 15A Seattle raw frames `[2, 7, 12, 17]`:

```text
learned contralateral range: 4.096565 to 4.272963
mean K6 reduction:           99.8184408838516%
minimum K6 reduction:        99.81753724223914%
mean selective reduction:    100.0%
mean removed-mass vs reduction correlation: 0.9811488931072522
```

Interpretation:

> In the pretrained HUGS Seattle sequence, learned cross-joint wrist/hand LBS components causally mediate the tested amplified contralateral upper-body response under left-wrist z perturbation, and this mechanism remains stable across four tested evaluation poses.

Do not generalize this yet to independently trained HUGS models, other joints/axes, or Gaussian-human methods.

## Step 16 cross-sequence setup

Official pretrained HUGS ZIP is persistent in Drive:

```text
/content/drive/MyDrive/interactive-digital-humans/assets/hugs/downloads/hugs_pretrained_models.zip
```

It contains official human checkpoints/configs for:

```text
citron
parkinglot
jogging
lab
bike
```

### Step 16B1

Official NeuMan archive probe:

```text
HTTP 206
Content-Range: bytes 0-0/4513980377
Accept-Ranges: bytes
archive size: 4.204 GiB
```

Byte-range random access is available. The full archive was not downloaded.

### Step 16B2

A range-backed ZIP reader found 6,075 entries and selectively extracted only the five candidate optimized SMPL pose files.

Persistent pose paths now exist for:

```text
citron
parkinglot
jogging
lab
bike
```

under:

```text
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/<sequence>/4d_humans/smpl_optimized_aligned_scale.npz
```

Candidate summary:

```text
citron:     37 frames, eval [2, 7, 12]
parkinglot: 42 frames, eval [2, 7, 12, 17]
jogging:   102 frames, eval [2, 7, 12, 17, 22, 27, 32, 37, 42, 47]
lab:       103 frames, eval [2, 7, 12, 17, 22, 27, 32, 37, 42, 47]
bike:      104 frames, eval [2, 7, 12, 17, 22, 27, 32, 37, 42, 47]
```

All five have constant betas across frames and expected NeuMan pose keys.

Compact artifact:

```text
experiments/07-cross-sequence-replication/analysis/16B2_pose_asset_inventory.csv
```

## First independent sequence

Use **parkinglot** first.

Reason: it has 42 frames and the same effective evaluation raw-frame indices `[2, 7, 12, 17]` as the Seattle frame-replication test. This provides a clean initial cross-sequence comparison while changing the pretrained model, learned deformation field, subject shape, canonical Gaussians, and source sequence.

This is a comparability choice, not a claim that parkinglot is more representative than the other NeuMan sequences.

## Exact next step

Run **Step 16B3 - parkinglot checkpoint/config extraction and provenance validation**.

Extract only from the existing pretrained-model ZIP:

```text
output/pretrained_models/parkinglot/human_final.pth
output/pretrained_models/parkinglot/config_train.yaml
```

Persist them under:

```text
/content/drive/MyDrive/interactive-digital-humans/assets/hugs/pretrained_models/parkinglot/
```

Then report:

1. SHA256 and file sizes,
2. packaged config values,
3. checkpoint top-level keys,
4. Gaussian `xyz` count,
5. triplane tensor shapes,
6. deformation-decoder tensor shapes.

Do not run the causal mechanism test yet. After provenance validation, reconstruct parkinglot learned LBS and K=6 target.

## Workflow and continuity

Continue exactly one Colab cell at a time. After each meaningful result:

- large arrays/checkpoints/datasets stay in Drive,
- compact CSV/JSON/Markdown/SVG go to GitHub,
- corrections are recorded explicitly,
- update the daily log, structured session checkpoint, this handoff, and `LATEST.md` as the continuation point advances.

For detailed chat continuity also read:

```text
research/sessions/2026-09-15.md
research/sessions/2026-09-15-step16b2.md
```
