# Research Session Checkpoint - 2026-09-15 Step 16D1

## Purpose

Prepare a third independently pretrained HUGS NeuMan checkpoint after establishing the Seattle/Parkinglot mechanism and Parkinglot axis/sign robustness.

Selected sequence: **jogging**.

## Official assets

Checkpoint/config were extracted from the already-persisted official pretrained-model ZIP.

```text
checkpoint: /content/drive/MyDrive/interactive-digital-humans/assets/hugs/pretrained_models/jogging/human_final.pth
config:     /content/drive/MyDrive/interactive-digital-humans/assets/hugs/pretrained_models/jogging/config_train.yaml
pose asset: /content/drive/MyDrive/interactive-digital-humans/datasets/neuman/jogging/4d_humans/smpl_optimized_aligned_scale.npz
```

## Provenance

```text
Jogging checkpoint size:   88.648732 MiB
Jogging checkpoint SHA256: 7a056fd6ba8ee9f5cc640eac43666e9ab57de33db2062a8ca379d1daa3afa769
Jogging config SHA256:     7e854af5d7d0dd564f863de4aca2465b8268c1eb3c3b06a0618c66cea8a25e53
Parkinglot SHA256:          f864f0fc3f4a9e6248fcd3ed0964b02967d7552825d6ff9993a149823204d76c
Jogging != Parkinglot:      True
```

This verifies a distinct independently pretrained checkpoint.

## Jogging NeuMan pose asset

```text
frames: 102
beta drift: 0.0
scale range: 1.4411990642547607 to 1.9300317764282227
effective evaluation raw frames: [2,7,12,17,22,27,32,37,42,47]
```

The expected keys are present: `global_orient`, `body_pose`, `betas`, `transl`, `scale`, `bbox`, and `vertex_colors`.

## Checkpoint geometry

```text
xyz shape: (311723, 3)
finite: True
Gaussian count: 311723
Parkinglot Gaussian count: 614157
```

The Gaussian population is sequence-specific and substantially different from Parkinglot.

## Architecture comparison

Jogging and Parkinglot have exact tensor-signature matches for all inspected core components:

```text
triplane:        exact signature match = True
geometry_dec:    exact signature match = True
deformation_dec: exact signature match = True
ALL CORE ARCHITECTURE MATCH: True
```

Triplane tensors are three `1x32x256x256` planes. The deformation decoder retains a 24-channel `skinning` output.

## Packaged config

```text
mode:                    human_scene
dataset.name:            neuman
dataset.seq:             jogging
human.name:              hugs_triplane
human.use_deformer:      True
human.disable_posedirs:  True
human.n_subdivision:     2
human.triplane_res:      256
human.canon_pose_type:   da_pose
human.optim_betas:       False
human.optim_pose:        True
human.optim_trans:       True
human.loss.lbs_w:        1000.0
```

Architecture-relevant config fields match Parkinglot exactly.

## Decision

```text
independent checkpoint: True
core architecture match: True
config identity valid: True
pose asset valid: True
THIRD CHECKPOINT READY: True
```

Jogging is therefore ready for a third-checkpoint learned-LBS reconstruction using the same validated HUGS source commit and forward path. No mechanism result has been observed for Jogging yet. Do not count this provenance step as a replication.

## Exact next action

Reconstruct Jogging canonical Gaussian xyz and learned 24-channel LBS directly from the official checkpoint using the already validated HUGS `TriPlane -> GeometryDecoder / DeformationDecoder -> softmax(lbs_logits/0.1)` path. Validate strict state loading, finiteness, row sums, and full-batch/chunk numerical agreement before constructing Jogging K=6 targets or applying perturbations.
