# Research Session Checkpoint — 2026-09-16 Step 18B0

## Purpose

Inventory the exact persistent artifacts and NPZ keys required to construct the predeclared left-shoulder frame-2 causal diagnostic without guessing file names or array layouts.

This step is preparatory only. It does not compute any shoulder perturbation displacement.

## Confirmed critical assets

All required SMPL, anatomy-mask, learned-LBS, K6-target, and NeuMan pose assets exist in Google Drive.

### Frozen anatomy masks

`experiments/08-second-joint-generalization/17A2_frozen_canonical_anatomy_masks.npz`

Contains sequence-specific frozen `valid`, `contralateral`, `dominant_joint`, and `confidence` arrays for Seattle, Parkinglot, and Jogging.

### Learned and K6 weights

Seattle:

- learned: `experiments/04-k6-support-mapping/13E_learned_lbs_weights.npz`
- K6: `experiments/04-k6-support-mapping/13A_k6_effective_mapping.npz`

Parkinglot:

- learned: `experiments/07-cross-sequence-replication/16C1_parkinglot_learned_lbs.npz`
- K6: `experiments/07-cross-sequence-replication/16C2_parkinglot_k6_effective_mapping.npz`

Jogging:

- learned: `experiments/07-cross-sequence-replication/16D2_jogging_learned_lbs.npz`
- K6: `experiments/07-cross-sequence-replication/16D3_jogging_k6_effective_mapping.npz`

The learned/K6 arrays retain the expected shapes `(G,24)` for each checkpoint.

## Important asymmetry discovered

Parkinglot and Jogging learned-LBS artifacts contain both `xyz_base` and `xyz_canon`.

Seattle `13E_learned_lbs_weights.npz` does **not** contain `xyz_canon`. Therefore the Seattle canonical Gaussian positions must be recovered from the earlier Seattle counterfactual/forward artifacts before Step 18B1 is constructed.

This is an input-recovery issue, not a scientific result and not a shoulder failure.

## Prior causal artifacts confirmed

Parkinglot:

- `16C3_parkinglot_counterfactual_displacements.npz`
- `16C4_parkinglot_frame_replication_displacements.npz`
- `16C5_parkinglot_axis_sign_displacements.npz`

Jogging:

- `16D4_jogging_counterfactual_displacements.npz`
- `16D5_jogging_frame_replication_displacements.npz`

Elbow:

- `17B1_left_elbow_frame2_displacements.npz`
- `17B2_left_elbow_full_pose_displacements.npz`

The Step-17 frame-2 artifact contains, per sequence, learned, K6, ablated displacement, and removed branch mass arrays. The full-pose artifact contains the corresponding learned/K6/ablated arrays for all 18 predeclared elbow poses.

## Search-root correction

The Step 18B0 inventory cell guessed two historical directory names incorrectly:

- guessed: `experiments/05-causal-counterfactual`
- actual: `experiments/05-counterfactual-lbs-ablation`

and

- guessed: `experiments/06-pose-robustness`
- actual: `experiments/06-frame-replication`

The incorrect roots did not alter any scientific result. Relevant Experiment 07/08 assets were still inventoried. The next inventory step must use the corrected historical directory names to recover the Seattle canonical-position source.

## Exact next action

Run Step 18B0A on CPU only:

1. search the corrected Seattle Experiment 04/05/06 Drive directories
2. identify every candidate array containing canonical or before/after Gaussian XYZ
3. inspect NPZ keys and shapes only
4. identify the exact Seattle canonical-position source used by prior causal work
5. do not run the shoulder perturbation yet

After Seattle canonical XYZ provenance is resolved, construct Step 18B1 with an explicit regression against the frozen Step-17 elbow frame-2 result before interpreting shoulder output.
