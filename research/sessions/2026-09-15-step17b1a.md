# Research Session Checkpoint - 2026-09-15 Step 17B1A

## Purpose

Resolve the remaining source-semantics caveat before running the full predeclared left-elbow pose schedules.

The audit used exact HUGS source commit:

`86ebe5522a384fc553f07f090b63a76dd4af8d33`

and compared Seattle, Parkinglot, and Jogging stored learned-LBS reconstructions against source-exact decoder reconstruction.

## Source findings

All three packaged configs report:

- `human.name: hugs_triplane`
- `human.activation: relu`
- `human.use_deformer: true`
- `human.disable_posedirs: true`
- `human.triplane_res: 256`
- `human.canon_pose_type: da_pose`

The located triplane HUGS implementation is `hugs/models/hugs_trimlp.py`, class `HUGS_TRIMLP`.

Its constructor creates:

```text
DeformationDecoder(n_features=n_features*3, disable_posedirs=disable_posedirs)
GeometryDecoder(n_features=n_features*3, use_surface=use_surface)
```

No activation argument is passed to either decoder.

The decoder class signatures are:

```text
GeometryDecoder(..., act='gelu')
DeformationDecoder(..., act='gelu', disable_posedirs=False)
```

Therefore the packaged config value `human.activation: relu` is not routed into these decoder constructors. Decoder execution uses their default GELU activation in this implementation.

The audit also directly verified source formulas for:

- `F.softmax(lbs_weights / 0.1, dim=-1)`
- canonical Gaussian coordinates `gs_xyz = self.get_xyz + xyz_offsets`
- `A_vitruvian2pose = A_t2pose @ self.inv_A_t2vitruvian`
- `disable_posedirs=True` causing zero pose offsets / `v_posed = v_shaped` in `lbs_extra`

## Numeric equivalence

Source-exact reconstruction matched the stored experimental arrays exactly.

| Checkpoint | LBS mean abs | LBS p99 | LBS max | Dominant mismatch | XYZ max where stored | Match |
|---|---:|---:|---:|---:|---:|---|
| Seattle | 0.0 | 0.0 | 0.0 | 0 | not stored | True |
| Parkinglot | 0.0 | 0.0 | 0.0 | 0 | 0.0 | True |
| Jogging | 0.0 | 0.0 | 0.0 | 0 | 0.0 | True |

The previously used learned-LBS arrays are therefore numerically identical to reconstruction under the located official decoder/forward semantics.

The cell reported:

```text
source structure resolved: True
all checkpoint numeric reconstructions match: True
SOURCE SEMANTICS EXACT: True
```

## Important precision note

The activation caveat is resolved. However, this audit located `HUGS_TRIMLP` heuristically from the source tree rather than explicitly traversing the config/model factory from the literal config string `hugs_triplane` to that class. Because the packaged config name and Python class/file name differ, one final lightweight alias/registry provenance check should explicitly establish the `hugs_triplane -> HUGS_TRIMLP` mapping before calling the full source provenance chain closed.

This does not invalidate the exact numeric match above. It is a provenance-link check, not a new reconstruction or endpoint change.

## Exact next action

Run Step 17B1B only:

1. find the exact official model factory/registry used by training/evaluation,
2. prove the literal config value `hugs_triplane` resolves to the located `HUGS_TRIMLP` implementation,
3. record the relevant source path and mapping,
4. do not alter any decoder, mask, threshold, perturbation, or causal endpoint.

If the alias mapping is confirmed, proceed to the already-predeclared full left-elbow pose schedules.