# Research Session Checkpoint - 2026-09-15 Step 16B1

This checkpoint supplements `research/sessions/2026-09-15.md` and preserves the newest ChatGPT/Colab research decision and output.

## User instruction carried forward

The user explicitly asked that research-conversation continuity be preserved on GitHub, not only final scientific artifacts. Continue saving structured records of decisions, corrections, numerical outputs, storage paths, and next actions so another chat can resume without losing context.

## Step 16B1 result

The official NeuMan archive URL documented by HUGS was probed without downloading the archive:

```text
https://docs-assets.developer.apple.com/ml-research/models/hugs/neuman_data.zip
```

Observed HTTP headers:

```text
status: 206
Content-Range: bytes 0-0/4513980377
Content-Length: 1
Accept-Ranges: bytes
Content-Type: application/zip
```

Archive size:

```text
4.204 GiB
4304.9 MiB
```

Byte-range requests are supported.

Available storage at the time of the probe:

```text
/content free:       65.01 GiB
/content/drive free: 61.76 GiB
```

No existing full NeuMan ZIP was present at either:

```text
/content/drive/MyDrive/interactive-digital-humans/assets/hugs/downloads/neuman_data.zip
/content/neuman_data.zip
```

## Decision

Do not download the full 4.2 GiB archive yet. Use random-access HTTP ranges to inspect the remote ZIP and selectively extract only the tiny candidate SMPL pose archives first. The target per sequence is:

```text
4d_humans/smpl_optimized_aligned_scale.npz
```

Persist those pose assets under the project Drive, inspect frame counts and keys, then choose the first independent HUGS sequence for the mechanism replication. Only after choosing a sequence should its matching pretrained checkpoint/config be extracted from the already-stored official HUGS pretrained ZIP.

## Exact next action

Step 16B2: open the remote NeuMan ZIP through a seekable range-backed file interface, locate the candidate pose NPZ members for `citron`, `parkinglot`, `jogging`, `lab`, and `bike`, selectively copy only those pose files into Drive, and inspect their shapes/frame counts.
