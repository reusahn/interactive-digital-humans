# Research Session Checkpoint - 2026-09-15 Step 16B2

This checkpoint extends the main [2026-09-15 session record](2026-09-15.md). It preserves the newest ChatGPT/Colab decisions and outputs needed to continue without the original chat.

## Immediate context

The Seattle mechanism result is already established and archived:

- Step 14E: learned cross-joint left-wrist/hand LBS channels causally mediate the tested Seattle contralateral upper-body response under left-wrist z +10 degrees.
- Step 15A: the mechanism is stable across Seattle raw frames `[2, 7, 12, 17]`.
- Step 16 goal: test an independently pretrained NeuMan/HUGS sequence before introducing a new regularizer.

## Step 16B1 - remote NeuMan archive probe

Official archive:

```text
https://docs-assets.developer.apple.com/ml-research/models/hugs/neuman_data.zip
```

Probe result:

```text
HTTP 206 Partial Content
Content-Range: bytes 0-0/4513980377
Accept-Ranges: bytes
archive size: 4.204 GiB
```

The archive supports byte-range access. Local free storage was above 60 GiB, but the acquisition strategy deliberately avoided downloading the full 4.2 GiB file.

## Step 16B2 - selective pose extraction

A range-backed remote ZIP reader opened the official NeuMan archive successfully:

```text
seekable: True
ZIP entries: 6075
optimized SMPL pose assets found: 6
```

Only five small candidate pose files were extracted to Drive. The full ZIP was not downloaded.

Persistent paths:

```text
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/citron/4d_humans/smpl_optimized_aligned_scale.npz
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/parkinglot/4d_humans/smpl_optimized_aligned_scale.npz
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/jogging/4d_humans/smpl_optimized_aligned_scale.npz
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/lab/4d_humans/smpl_optimized_aligned_scale.npz
/content/drive/MyDrive/interactive-digital-humans/datasets/neuman/bike/4d_humans/smpl_optimized_aligned_scale.npz
```

Candidate summary:

| Sequence | Frames | Beta drift | Scale range | Effective evaluation raw frames |
|---|---:|---:|---|---|
| citron | 37 | 0 | 2.503076 to 3.338921 | `[2, 7, 12]` |
| parkinglot | 42 | 0 | 3.538034 to 4.013123 | `[2, 7, 12, 17]` |
| jogging | 102 | 0 | 1.441199 to 1.930032 | `[2, 7, 12, 17, 22, 27, 32, 37, 42, 47]` |
| lab | 103 | 0 | 8.232719 to 9.435638 | `[2, 7, 12, 17, 22, 27, 32, 37, 42, 47]` |
| bike | 104 | 0 | 1.459522 to 1.607994 | `[2, 7, 12, 17, 22, 27, 32, 37, 42, 47]` |

All five files contain the expected keys and constant betas across frames.

Compact GitHub artifact:

```text
experiments/07-cross-sequence-replication/analysis/16B2_pose_asset_inventory.csv
```

## Decision: first independent sequence

Use **parkinglot** first.

Reason:

- independently pretrained official HUGS checkpoint,
- 42 pose frames, close to Seattle's 41,
- effective evaluation raw frames `[2, 7, 12, 17]`, exactly matching the four raw-frame indices used for Seattle Step 15A,
- therefore gives a clean initial cross-sequence comparison while changing sequence, checkpoint, learned deformation field, shape, and canonical Gaussian set.

This choice is for controlled comparability, not because parkinglot is assumed to be more representative than the other NeuMan sequences.

## Exact next action

Run **Step 16B3** only:

1. extract `output/pretrained_models/parkinglot/human_final.pth` from the existing Drive `hugs_pretrained_models.zip`,
2. extract `output/pretrained_models/parkinglot/config_train.yaml`,
3. save them under `assets/hugs/pretrained_models/parkinglot/`,
4. compute SHA256 and file sizes,
5. inspect config and checkpoint structure,
6. report Gaussian count and important decoder/triplane tensor shapes,
7. do not yet run the mechanism ablation.

Continue one Colab cell at a time.
