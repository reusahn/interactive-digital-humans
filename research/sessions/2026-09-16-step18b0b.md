# Research Session Checkpoint — 2026-09-16 Step 18B0B

## Purpose

Prove exact elementwise identity of the persistent Seattle baseline-probe canonical Gaussian coordinates before constructing the predeclared shoulder causal diagnostic.

This validation read only `xyz_canon` and `xyz_before`. It intentionally did not read `xyz_after` and did not compute any shoulder causal metric.

## Reference fixed

Authoritative Seattle canonical-source reference:

`experiments/01-baseline/probe_results/frame000_left_wrist_z_10deg.npz`

Reference arrays:

- `xyz_canon`: shape `(472958,3)`, dtype `float32`
- `xyz_before`: shape `(472958,3)`, dtype `float32`

Hashes:

```text
xyz_canon SHA256: 61a633d5b2c1fb353d7790cdd176919f17c3f1000ce7e9cb5d03e2751b51d314
xyz_before SHA256: 291a1fcb5818b35ed0ecf6acdb461d2532fd4ce0323be3b10e3d12f67c075c73
```

## Exact validation result

Thirty-seven persistent copies were checked: all 36 current baseline-probe files plus the historical duplicate under `experiment-01/20260914_003959/`.

Every file passed all checks:

```text
all shape OK: True
all dtype OK: True
all finite: True
all xyz_canon elementwise exact: True
all xyz_canon SHA256 match: True
all xyz_before elementwise exact: True
all xyz_before SHA256 match: True
SEATTLE CANONICAL XYZ EXACTLY VALIDATED: True
```

For every candidate, both `xyz_canon` and `xyz_before` had maximum absolute difference `0.0` from the reference.

## Interpretation

Seattle canonical Gaussian provenance is now closed for the Step-18 computation. The baseline-probe family does not contain competing canonical coordinate states. One fixed reference file can be used without introducing a joint-specific or perturbation-specific coordinate choice.

This result does not inspect or establish any shoulder causal effect.

## Drive artifact

`experiments/08-second-joint-generalization/18B0B_seattle_xyz_exact_validation.json`

## Exact next action

Construct Step 18B1 using the frozen third-joint protocol. Before interpreting any shoulder result, the computation must first reproduce the frozen Step-17 elbow frame-2 aggregates across Seattle, Parkinglot, and Jogging. If the regression fails, stop before interpreting shoulder output.
