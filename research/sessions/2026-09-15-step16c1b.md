# Research Session Checkpoint - 2026-09-15 Step 16C1B

## Context

Parkinglot checkpoint provenance and architecture were already validated. Step 16C1 strict-loaded the official checkpoint and reconstructed canonical xyz plus learned 24-channel LBS over all 614,157 Gaussians. A first 4,096-point rerun differed from the 65,536-point chunked pass by up to `3.9041e-6` in learned LBS, so a full-batch audit was run before accepting the export.

## Step 16C1B result

The actual HUGS-style all-Gaussian call shape was reproduced by forwarding all `614,157` Gaussians in one CUDA batch.

Authoritative validation:

```text
xyz_canon shape: (614157, 3)
learned_lbs shape: (614157, 24)
xyz NaN: 0
LBS NaN: 0
xyz Inf: 0
LBS Inf: 0
max LBS row-sum error: 3.5762786865234375e-07
mean LBS row-sum error: 5.003608194442677e-08
```

Full-batch versus the previously exported 65,536-chunk pass:

```text
canonical xyz absolute difference: all quantiles 0, max 0
learned-LBS element difference:    all quantiles 0, max 0
per-Gaussian LBS L1 difference:    all quantiles 0, max 0
mean per-Gaussian LBS L1:          0
geometry-offset norm difference:   0
```

Anatomical stability was exact:

```text
dominant-joint mismatch count: 0
dominant-joint mismatch fraction: 0
high-confidence >=0.9 mismatch count: 0
chunk high-confidence count: 290822
full high-confidence count: 290822
left-wrist/hand channel difference: 0 at every reported quantile
```

Final decision:

```text
FULL-BATCH FORWARD VALID: True
CHUNK SEMANTIC STABILITY: True
STEP 16C1B AUDIT PASS: True
```

The earlier `3.9041e-6` discrepancy therefore came from rerunning only 4,096 points with a different CUDA batch shape. It is not evidence of an incorrect reconstruction. The 65,536-chunk pass and the actual full-batch HUGS call are exactly identical in this runtime.

## Authoritative Drive artifacts

```text
/content/drive/MyDrive/interactive-digital-humans/experiments/07-cross-sequence-replication/16C1_parkinglot_learned_lbs.npz
/content/drive/MyDrive/interactive-digital-humans/experiments/07-cross-sequence-replication/16C1B_fullbatch_numerical_audit.json
```

The authoritative NPZ contains:

- checkpoint base xyz,
- reconstructed canonical xyz,
- learned 24-channel LBS,
- dominant joint,
- dominant confidence,
- geometry-offset norm.

## Scientific status

Parkinglot now has a validated learned-deformation export from an independently pretrained official HUGS checkpoint. No K=6 target or perturbation result has been computed yet, so the Seattle causal result must still not be generalized to Parkinglot or HUGS as a method.

## Exact next action

Construct the Parkinglot-specific SMPL-derived K=6 target using the Parkinglot betas and the same HUGS K=6 weighting rule used for Seattle. Validate the reconstructed anatomical assignment and target row sums before running the left-wrist perturbation or selective ablation.
