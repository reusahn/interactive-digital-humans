# Research Session Checkpoint - 2026-09-15 Step 17B1C

## Purpose

Resolve the provenance discrepancy discovered after the failed Step 17B1B alias assumption.

## Exact source commit

`86ebe5522a384fc553f07f090b63a76dd4af8d33`

## Exact commit string audit

```text
tracked `hugs_triplane` hits: 0
tracked `hugs_trimlp` hits: 8
```

The `hugs_trimlp` hits include the official release/ablation configs and trainer source.

## Official release configuration

`cfg_files/release/neuman/hugs_human_scene.yaml` reports:

```text
human.name: hugs_trimlp
human.activation: relu
```

## Official trainer mapping

The trainer:

- imports `HUGS_TRIMLP`
- checks `cfg.human.name == 'hugs_trimlp'`
- constructs `HUGS_TRIMLP(...)`
- does not check `cfg.human.name == 'hugs_triplane'`

Therefore:

```text
RELEASED TRAINER hugs_trimlp -> HUGS_TRIMLP: True
```

## Packaged pretrained configurations

The downloaded pretrained package configs for all three tested checkpoints report:

```text
Seattle    human.name = hugs_triplane
Parkinglot human.name = hugs_triplane
Jogging    human.name = hugs_triplane
```

All three also report `activation = relu`.

## Link to Step 17B1A

Step 17B1A had already established:

```text
source_semantics_exact: True
all_numeric_match: True
```

The stored learned-LBS arrays numerically match reconstruction under released `HUGS_TRIMLP` semantics exactly.

## Provenance decision

```text
released source contains hugs_trimlp: True
released source contains hugs_triplane: False
official release config uses hugs_trimlp: True
packaged configs all use hugs_triplane: True
trainer exact hugs_trimlp -> HUGS_TRIMLP: True
no released hugs_triplane alias: True
Step 17B1A source semantics exact: True
Step 17B1A numeric match: True
PACKAGED CONFIG LABEL DRIFT RESOLVED: True
```

## Correct interpretation

The exact released source uses model name `hugs_trimlp` and directly constructs `HUGS_TRIMLP`. The downloaded pretrained package configs contain the different literal `hugs_triplane`, and no executable alias for that literal exists in the released commit.

This should be described as a packaged-config naming/label mismatch or drift. It should **not** be described as a `hugs_triplane -> HUGS_TRIMLP` alias.

The historical reason the packaged configs carry the different label is not established by this audit. Do not claim a specific packaging history such as 'legacy' unless separately proven.

## Impact

No reconstruction rerun is indicated. No experimental endpoint, mask, threshold, perturbation, or causal result changes.

The Step 17B1 left-elbow frame-2 causal result remains valid under the tested reconstruction semantics.

## Failed-assumption record

The failed Step 17B1B alias assumption is preserved separately in:

`research/sessions/2026-09-15-step17b1b-assumption-failure.md`

and cumulatively in:

`research/methodology/assumption-failure-ledger.md`

## Exact next action

Proceed to the already-predeclared full left-elbow pose schedule using the frozen Step-17A2 masks and unchanged diagnostic thresholds:

- Seattle `[2,7,12,17]`
- Parkinglot `[2,7,12,17]`
- Jogging `[2,7,12,17,22,27,32,37,42,47]`

This is the next scientific test. The provenance naming issue is documented and no longer blocks the pose-robustness experiment.
