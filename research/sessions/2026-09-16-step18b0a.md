# Research Session Checkpoint — 2026-09-16 Step 18B0A

## Purpose

Recover the exact persistent Seattle canonical-Gaussian position source needed before constructing the predeclared left-shoulder causal test.

Step 18B0 had already established that Parkinglot and Jogging learned-LBS artifacts contain `xyz_canon`, while Seattle `13E_learned_lbs_weights.npz` does not.

## Result

The corrected Experiment 01-06 search roots were present. The inventory found 43 numeric artifacts and 111 arrays with exact Seattle Gaussian position shape `(472958, 3)`.

The original Seattle probe artifacts under

`experiments/01-baseline/probe_results/`

contain

- `xyz_before` `(472958,3)` float32
- `xyz_after` `(472958,3)` float32
- `xyz_canon` `(472958,3)` float32
- region/anatomy metadata

The historical duplicate

`experiment-01/20260914_003959/frame000_left_wrist_z_10deg.npz`

contains the same array schema.

Every reported `xyz_canon` candidate in the inventory had the same summary statistics:

```text
shape:    (472958, 3)
dtype:    float32
min:      -0.8927599191665649
max:       0.8819881677627563
mean xyz: [-0.023014819249510765,
           -0.12770146131515503,
            0.013593386858701706]
```

This identifies the Seattle baseline probe artifacts as the persistent source family for the canonical Gaussian coordinates used by the original diagnostic pipeline.

However, identical summary statistics alone do not prove elementwise identity across all copies. One exact equality validation is therefore required before fixing the source path for Step 18B1.

## Known causal-artifact structure reconfirmed

`14E_corrected_counterfactual_displacements.npz` stores scalar per-Gaussian learned/K6/selective-ablation displacement magnitudes plus anatomy/mask metadata, not canonical XYZ.

`15A_seattle_frame_replication_displacements.npz` stores scalar per-Gaussian learned/K6/ablated displacement arrays for frames 2, 7, 12, and 17, also without canonical XYZ.

Therefore canonical positions must come from the original baseline probe artifact family, while the causal conditions continue to be recomputed from frozen learned/K6 weights and pose transforms.

## Methodological note

The Step 18B0A inventory was intended as provenance-only inspection, but its generic `(472958,3)` candidate printer also printed min/max/mean summaries for `xyz_after`, including the pre-existing Seattle `left_shoulder z +10°` baseline probe.

This exposed aggregate perturbed-coordinate summaries, although it did **not** compute or display the predeclared causal metrics: contralateral displacement sum, K6 reduction, selective-ablation reduction, removed-mass correlation, or pass/fail status.

No joint, branch, mask, threshold, axis, angle, checkpoint, or pose schedule is changed. The frozen Step-18 shoulder protocol remains unchanged. Future provenance cells should avoid printing perturbation-output summaries unless they are the intended result.

## Saved Drive artifact

`experiments/08-second-joint-generalization/18B0A_seattle_xyz_source_inventory.json`

## Exact next action

Run Step 18B0B on CPU only to establish elementwise identity of the Seattle `xyz_canon` copies and fix one authoritative source path. Do not inspect `xyz_after` in that validation.

Only after exact canonical-XYZ validation should Step 18B1 run the frozen shoulder causal computation.