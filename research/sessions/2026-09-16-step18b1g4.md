# Research Session Checkpoint — 2026-09-16 Step 18B1G4

## Purpose

Test whether the remaining frozen Step-17B1 elbow regression mismatch is primarily due to GPU architecture / low-level CUDA kernel selection by rerunning the same legacy HUGS software stack on a second GPU architecture.

Runtime:

- GPU: `Tesla T4`
- Python: `3.8.20`
- NumPy: `1.24.4`
- PyTorch: `1.13.1+cu117`
- CUDA build: `11.7`
- SMPLX: `0.1.28`
- TF32 disabled
- elbow frame 2 only
- frozen aggregate regression gate unchanged at `1e-5`

## Result

| Checkpoint | Learned error | Learned gate | K6 error | K6 gate | Kinematics |
|---|---:|---|---:|---|---|
| Seattle | `-9.328914529760368e-08` | PASS | `+1.3674934962182306e-06` | PASS | PASS |
| Parkinglot | `+3.830264290627383e-05` | FAIL | `-1.8405622768113972e-06` | PASS | PASS |
| Jogging | `+1.361315298709087e-06` | PASS | `0.0` | PASS | PASS |

```text
learned passes: 2/3
K6 passes: 3/3
total passes: 5/6
kinematic passes: 3/3
T4 LEGACY ENV REGRESSION RECOVERY: False
```

The T4 changes the tiny aggregate errors materially relative to the A100 under the same legacy software stack, but does not recover Parkinglot learned within the frozen `1e-5` criterion. The Parkinglot learned error changes from about `-2.87e-05` on A100 legacy to `+3.83e-05` on T4 legacy. Seattle moves essentially onto the archive (`-9.33e-08`) and Jogging remains well inside the gate.

Per-Gaussian spatial agreement remains effectively exact for the scientific purpose, with learned Pearson correlations:

- Seattle `0.9999999999318491`
- Parkinglot `0.999999999976513`
- Jogging `0.9999999998511598`

## Interpretation

The strict historical aggregate `1e-5` criterion is not portable across the tested numerical runtimes. Both A100 and T4 under the same source-declared legacy software stack produce `5/6`, while the sign and magnitude of the remaining Parkinglot residual change with hardware. This is positive evidence for low-level floating-point runtime sensitivity rather than a source-semantics or scientific-field mismatch.

The historical gate must not be silently widened. It remains a failed exact-reproduction criterion for the currently available runtimes. Further GPU hunting is not justified as the primary research path unless exact historical hardware provenance is later recovered.

No shoulder causal displacement was computed in this audit.

## Artifact note

The T4 G4 runner reuses the G3 output filename:

`experiments/08-second-joint-generalization/18B1G3_legacy_torch113_cuda117_regression.json`

Therefore the current Drive file now contains the T4 result. The A100 legacy values are preserved in `research/sessions/2026-09-16-step18b1g3.md`.

## Continuation policy

Before inspecting shoulder causal displacement, freeze a runtime-specific continuation policy rather than changing the historical regression gate:

1. preserve historical `1e-5` elbow exact-reproduction status as failed on available runtimes
2. use one fully specified runtime as the primary Step-18 execution environment
3. keep all already-frozen shoulder scientific thresholds unchanged
4. treat the primary shoulder result as valid only under the declared runtime
5. later perform a second-runtime sensitivity check on the shoulder pass/fail conclusions, not by tuning thresholds

Given the current active runtime and source-declared HUGS stack, the proposed primary continuation runtime is Tesla T4 + Python 3.8 + PyTorch 1.13.1+cu117 + SMPLX 0.1.28 + TF32 disabled.
