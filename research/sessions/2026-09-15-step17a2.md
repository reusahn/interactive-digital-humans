# Research Session Checkpoint - 2026-09-15 Step 17A2

## Purpose

Freeze one canonical anatomical-mask source per checkpoint before any predeclared left-elbow perturbation, and recompute the descriptive left-elbow precursor with those frozen masks.

The second-joint protocol remains unchanged:

- joint: `left_elbow` (`SMPL 18`)
- axis reserved for causal test: `z`
- perturbation reserved for causal test: `+10 deg`
- branch channels: `{18,20,22}`
- contralateral anatomy: `{14,17,19,21,23}`
- confidence threshold: `>= 0.9`
- no elbow displacement has been computed yet

## Canonical anatomy sources now frozen

- Seattle: `saved_dominant` + `saved_confidence` from `13A_k6_effective_mapping.npz`
- Parkinglot: `dominant_joint` + `joint_confidence` from `16C2_parkinglot_k6_effective_mapping.npz`
- Jogging: `dominant_joint` + `joint_confidence` from `16D3_jogging_k6_effective_mapping.npz`
- K6 deformation weights remain the checkpoint-specific `effective_lbs` arrays.

This source mapping was fixed before any left-elbow perturbation.

## Canonical precursor results

| Checkpoint | HC | Contra | Contra dominant agreement | K6 branch mean | Learned branch mean | Learned/K6 ratio | Learned >1e-3 | K6 >1e-3 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Seattle | 197778 | 33072 | 1.000000 | 5.82675398e-7 | 4.13076516e-4 | 708.930765x | 0.0953979 | 0.0 |
| Parkinglot | 292095 | 77622 | 0.999948 | 2.16326697e-7 | 2.39837973e-4 | 1108.684116x | 0.0502048 | 0.0 |
| Jogging | 148392 | 20887 | 0.999952 | 2.76371622e-7 | 3.74563533e-4 | 1355.289410x | 0.0780390 | 0.0 |

All expected anatomy counts match the previously reviewed checkpoint benchmarks. Learned branch mean remains larger than K6 branch mean in all three checkpoints.

## Seattle mask correction magnitude

Using the recomputed `argmax/max(effective_lbs)` mask had included exactly two additional contralateral rows:

- index `239232`: saved confidence `0.81323647`, recomputed confidence `0.91301531`, dominant joint 21 in both; K6 branch mass `0`, learned branch mass `1.25682927e-5`
- index `269473`: saved confidence `0.88171196`, recomputed confidence `0.94597453`, dominant joint 21 in both; K6 branch mass `0`, learned branch mass `5.59534310e-5`

The canonical-mask correction changes Seattle means only minimally:

```text
K6 branch mean:
recomputed mask  5.82640155e-7
canonical mask   5.82675398e-7
delta            3.52429e-11

learned branch mean:
recomputed mask  4.13053611e-4
canonical mask   4.13076516e-4
delta            2.29047e-8
```

Therefore the qualitative pre-perturbation precursor conclusion is unchanged by the mask correction.

## Decision

```text
all expected anatomy counts match: True
learned branch mean > K6 in all checkpoints: True
Seattle precursor ordering preserved after mask correction: True
CANONICAL ANATOMY SOURCES FROZEN: True
```

The Seattle saved-vs-recomputed confidence discrepancy is provenance-reconciled for implementation, although the deeper historical reason those stored confidences differ from `max(effective_lbs)` is not yet established. That unresolved origin does not change the frozen source mapping.

## Saved Drive artifacts

- `experiments/08-second-joint-generalization/17A2_left_elbow_canonical_precursor.csv`
- `experiments/08-second-joint-generalization/17A2_canonical_anatomy_source_manifest.json`
- `experiments/08-second-joint-generalization/17A2_frozen_canonical_anatomy_masks.npz`

## Exact next action

Run the predeclared left-elbow causal diagnostic on raw frame 2, using the now-frozen canonical masks and unchanged branch intervention `{18,20,22}`. Prefer a shared frame-2 cross-checkpoint test before expanding to the full nested pose schedules.
